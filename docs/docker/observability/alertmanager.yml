global:
  resolve_timeout: 5m

route:
  receiver: spug-webhook
  group_by: [alertname, spug_host_id]
  group_wait: 15s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: spug-webhook
    webhook_configs:
      - url: http://spug/api/v1/observability/alertmanager/webhook/
        send_resolved: true
        max_alerts: 500
        http_config:
          authorization:
            type: Bearer
            credentials_file: /run/secrets/alertmanager_webhook_token
