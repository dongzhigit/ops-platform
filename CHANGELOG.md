# Changelog

## 0.1.0-alpha.14 - 2026-08-19

- AI 模型页面允许回环地址、Docker 内部主机名和 RFC1918 内网 IP 在生产模式下使用 HTTP，适配无 TLS 的隔离内网模型网关。
- 公网域名和公网 IP 的 HTTP 地址继续被拒绝；校验不根据普通 DNS 的临时解析结果放行，避免 DNS 重绑定绕过。
- 页面提示和部署文档同步区分公网 HTTPS 与可信内网 HTTP，并明确内网 HTTP 不提供传输加密。
- MariaDB 完整 110 项测试通过（2 项外部条件跳过），React 生产构建通过。

## 0.1.0-alpha.13 - 2026-08-19

- AI 模型页面新增 API 格式选择，支持 OpenAI Chat Completions 与 Anthropic Messages 原生协议并即时切换。
- OpenAI 格式使用 `/chat/completions`、Bearer Token 和可选 JSON Object 模式；Anthropic 格式使用 `/messages`、`x-api-key`、固定 API 版本头和顶层系统提示。
- 新增 Anthropic 文本内容块及 `input_tokens`/`output_tokens` 解析；拒绝模型返回的工具或其他非文本内容块，既有只读证据与引用校验边界保持不变。
- 环境回退新增 `SPUG_AIOPS_API_FORMAT=openai|anthropic`，已有页面配置迁移后默认保持 OpenAI 格式。

## 0.1.0-alpha.12 - 2026-08-19

- AI 运维页面新增模型配置弹窗，可即时管理启用状态、OpenAI-compatible 地址、模型、JSON mode、超时、证据上限、输出上限、响应字节上限和调用频率，无需重启容器。
- 新增独立 `aiops.config.manage` 权限；配置修改写入 HMAC 审计链，普通调查用户只能看到脱敏配置状态。
- 页面提交的 API Key 使用现有 AES-GCM 信封加密与凭据主密钥落库，查询接口永不返回明文或密文；主密钥轮换命令同步覆盖 AI 模型密钥。
- 数据库页面配置优先于环境变量，环境密钥文件继续作为启动和灾难恢复回退；生产模式仍强制 HTTPS，模型仍无任何执行工具。

## 0.1.0-alpha.11 - 2026-08-19

- 新增知识空间、viewer/editor/manager 成员角色和页面权限双重授权，支持文档、Runbook、故障复盘、资产说明及草稿/审核/发布/归档状态。
- 文档每次有效修改与版本恢复均创建不可覆盖的新版本；删除采用软删除，空间产生文档后固定引用标识，知识写操作进入 HMAC 审计链。
- 新增授权全文检索，AI 证据只召回当前用户可访问的已发布知识，并使用 `knowledge://<space>/<document>?v=<version>` 固定引用。
- 新增默认关闭的 OpenAI-compatible 只读 AI 调查，证据范围包含授权主机、Prometheus 指标、告警和知识片段；结果保存证据快照、模型、提示版本、token 用量及审计关联 ID。
- 模型请求不携带工具或函数；后端递归拒绝命令/脚本/工具字段并校验事实引用，行动方案固定不可执行；生产模型地址强制 HTTPS，密钥通过独立只读文件注入，响应体使用流式字节上限。
- 新增知识库与 AI 前端、角色权限和安全审计动作展示；所有知识与模型内容均按纯文本渲染。
- MariaDB 10.11 全量 105 项测试通过（2 项外部条件跳过），空库迁移、模型漂移、Django system check、React 生产构建、Compose 渲染、Gitleaks 和 Trivy 复扫通过。
- 最终镜像仍有 3 个严重、203 个高危漏洞项，CentOS 7/Python 3.6/Django 2.2 以及 Guacamole Java 依赖未完成扫描继续阻断正式生产和公网部署。

## 0.1.0-alpha.10 - 2026-08-18

- 新增 Prometheus 服务发现、主机 CPU/内存/磁盘/网络预定义查询和 1/6/24 小时趋势，采集目标同时受页面权限与主机对象授权约束。
- 新增 Alertmanager Bearer Token 回调、告警 episode 聚合/恢复、认领、真实 Silence 和时区校验；监控密钥采用独立只读文件注入。
- 增加主机指标总览、采集目标和指标告警工作台，并完成真实 Compose 监控链路验证。
- MariaDB 91 项测试通过（2 项外部条件跳过）；Prometheus 与 Alertmanager 最终镜像无严重/高危项，node-exporter 官方最新镜像仍有 8 个可修复高危项。

## 0.1.0-alpha.9 - 2026-08-18

- 新增 RDP/VNC 端点和远程访问会话模型，端点只能引用已绑定到主机的同协议启用身份及加密密码凭据；Guacamole 参数采用协议白名单，禁止覆盖目标和凭据。
- 新增 `rdp.connect`、`vnc.connect` 对象动作和 `host.console.remote` 页面权限；票据签发和启动时都会复核用户、页面权限、对象授权、端点、身份、凭据、绑定与协议，端点配置变化会使已签发票据失效。
- 启动票据使用 32 字节随机值，数据库只保存 SHA-256，默认 60 秒过期且只能消费一次；Scheduler 每 5 分钟清理未使用票据。
- 按 Guacamole 官方格式实现 UTF-8 JSON、HMAC-SHA256、AES-128-CBC/零 IV/PKCS#7 和 Base64 加密，长期密码只在服务端内存中短时解密，不返回前端或写入会话/审计数据。
- 主机列表新增远程桌面窗口和管理员端点配置；Compose 固定 Guacamole/guacd 1.6.0、隔离服务网络、健康检查、真实客户端 IP 传递及 `/guacamole/` 反向代理，不发布网关端口。
- 启动和 Guacamole 查询字符串在内置 Nginx 中关闭访问日志；文档明确加密 JSON 在短 TTL 内仍可能重放，以及当前不含真实连接状态回调、强制断开或录像。
- MariaDB 82 项完整回归（2 项外部集成按条件跳过）、空库迁移、模型漂移、React 生产构建及官方 Guacamole JSON 认证容器集成测试通过。
- Spug 镜像仍有 3 个严重、203 个高危；Guacamole Web 操作系统层有 11 个可修复高危，guacd 的 Alpine 3.18 已停止维护，Java 依赖扫描尚未完成，因此继续阻断正式生产和公网部署。

## 0.1.0-alpha.8 - 2026-08-18

- SFTP 文件管理增加受审批的在线文本编辑：仅接受最大 2 MiB 的 UTF-8 普通文件，拒绝 NUL 二进制内容和符号链接。
- 同一主机与规范化路径只保留一个持久化编辑锁，锁有效期 30 分钟，页面每 5 分钟续期；取消、页面卸载和过期会释放锁并清理随机临时文件。
- 审批票据精确绑定主机、路径、基础 ETag/SHA-256、新内容大小/SHA-256、UTF-8 编码和拒绝覆盖冲突策略；远端文件在审批消费前变化时不会消费票据。
- 保存使用随机同目录临时文件，校验远端摘要，保留原文件 POSIX mode、UID 和 GID，再次检查基础版本后安全替换；失败会消费已开始写入的审批并记录脱敏审计。
- Scheduler 每 15 分钟清理已取消或过期的编辑会话；新增 `file.0004_fileeditsession` 迁移、Ace 编辑弹窗、锁心跳与主机切换释放。
- MariaDB 10.11 全量 69 项测试通过，真实 OpenSSH SFTP 覆盖在线读取、审批保存、摘要校验及 mode/UID/GID 保留；Python 3.6 编译、迁移漂移检查和 React 生产构建通过。

## 0.1.0-alpha.7 - 2026-08-17

- 增加持久化 SFTP 上传批次，一批支持 1 至 50 个文件、总计最多 5 TiB、服务端受控 1 至 4 路并发；当前浏览器固定使用 2 路文件并发和 4 MiB 顺序分片。
- 一张一次性审批单精确绑定排序后的整批文件名、大小、SHA-256、主机、目录、分片大小、冲突策略和并发数；批次子文件共享审批关联，但各自保留独立偏移与远端临时路径。
- 文件管理页面增加多文件选择、逐文件摘要计算、批次总进度与单文件进度面板，以及暂停、恢复、失败清理后重试和取消；相同文件集可通过本地批次 UUID 恢复，不会重复消费审批。
- 文件列表增加多选和批量下载入口，明确提示浏览器可能要求允许多个下载；每个下载继续使用单文件 HTTP Range 链路。
- 批次状态聚合、过期与取消清理接入最终审计；不同文件只锁定各自传输行，避免两路上传被批次或主机行锁退化为串行。
- MariaDB 10.11 全量 61 项测试通过，其中真实 OpenSSH SFTP 测试覆盖两文件并发、暂停恢复、远端损坏失败、清理重试、取消和 Scheduler 删除；空库迁移、React 构建、Nginx/API、镜像运行、Gitleaks 和 Trivy 验证同步执行。

## 0.1.0-alpha.6 - 2026-08-17

- SFTP 下载支持单段 HTTP Range、开放范围和后缀范围，并返回 `Accept-Ranges`、`Content-Range`、`ETag`、`Last-Modified`；支持 `If-Range`、HEAD、206 和 416 语义。
- 下载保持限长流式读取，关闭 Nginx 代理缓冲；完整传输、客户端中断和远端文件打开失败分别写入成功或失败审计事件。
- 上传会话增加临时文件清理状态、尝试次数和时间记录；取消上传时立即清理，过期、失败或取消会话由 Scheduler 每 15 分钟重试，单会话最多 10 次。
- 增加默认只读的 `cleanup_file_transfers` 管理命令，支持显式 `--execute`，并区分已删除、文件不存在和权限/网络失败。
- 增加 11 项 Range 下载、HEAD、中断审计、清理重试、管理命令和 Scheduler 回归测试，MariaDB 全量测试增至 47 项；真实 OpenSSH SFTP、Nginx 代理、空库迁移、镜像运行和 Gitleaks 验证通过。

## 0.1.0-alpha.5 - 2026-08-17

- SSH 文件管理改用持久化上传会话，浏览器按 4 MiB 分片发送；网络或页面中断后，重新选择同一文件可在 24 小时内从服务端记录的偏移量续传。
- 上传审批绑定主机、路径、文件名、大小、分片大小、冲突策略及整文件 SHA-256；审批消费前只在浏览器本地计算摘要，不发送文件内容。
- 每个分片在写入远端临时文件前校验 SHA-256，完成时重新流式读取远端文件并校验整文件摘要，通过后才替换正式文件。
- OpenSSH 服务端优先使用原子覆盖；不支持扩展时使用带失败恢复的临时备份，校验或权限错误不会覆盖原目标文件。
- 增加上传会话所有权、精确偏移、防重复分片、摘要篡改、远端校验失败及安全覆盖测试，MariaDB 集成测试增至 36 项。

## 0.1.0-alpha.4 - 2026-08-17

- SSH 文件管理上传接入通用审批门禁，批准前不发送文件内容或启动进度通道，申请提交后保留所选文件并可刷新审批状态。
- SSH 文件删除接入严重风险审批，强制填写回滚方案并使用一次性票据。
- 操作执行失败后重新向服务端预检票据，避免继续展示已消费或过期的审批单。
- 增加 SFTP 上传/删除的拒绝、精确参数绑定、一次性消费和审计事件测试，MariaDB 集成测试增至 32 项。

## 0.1.0-alpha.3 - 2026-08-17

- 增加“安全中心”，提供我的申请、待我审批和只追加审计日志页面。
- 增加服务端操作预检接口，统一返回风险等级、回滚要求及完全匹配的有效审批单。
- 增加通用 React 审批门禁，前端不计算或信任审批参数哈希。
- 批量命令和文件分发页面接入审批申请、票据选择及执行流程。
- 计划任务创建/编辑、启用、手动测试和删除接入独立的一次性审批票据。
- 增加审批预检的风险、权限、参数匹配、过期和代理路由测试，MariaDB 集成测试增至 31 项。

## 0.1.0-alpha.2 - 2026-08-14

- 增加高风险操作服务端风险分级、双人审批、过期控制和一次性批准票据。
- 审批绑定动作、目标及参数哈希，阻止参数篡改和票据重放。
- 增加只追加的 HMAC-SHA256 审计事件链及完整性检查命令。
- 批量命令、文件上传/删除/分发和计划任务接入执行前与 Worker 二次校验。
- 原有发布审批增加申请人/审批人分离，并桥接统一审计事件。
- 生产环境增加独立的 `SPUG_AUDIT_SIGNING_KEY`。

## 0.1.0-alpha.1 - 2026-08-14

首个内部预发布版本，基于 OpenSpug v3.4.0 二次开发。

- 建立私有化运维平台分阶段蓝图和生产安全门禁。
- 外置生产密钥、数据库口令、域名和 Cookie/TLS 安全配置。
- 增加凭据 AES-GCM 信封加密、身份绑定、对象级 allow/deny 与临时授权。
- SSH、SFTP、批量命令、文件分发、计划任务、监控和发布接入动作授权。
- 修复主机私钥返回浏览器及批量任务明文私钥写入 Redis 的问题。
- 增加旧主机私钥迁移、双密钥分阶段轮换及回读验证命令。
- 固定全部 Django 应用迁移基线，生产启动禁止动态生成迁移。
- Docker 镜像改为编译当前仓库后端和前端，不再运行时下载上游源码。
- 通过 8 项环境配置测试、11 项 MariaDB M1 集成测试、迁移空库安装、Gitleaks 和容器启动验证。

已知限制：CentOS 7、Python 3.6、Django 2.2 和部分依赖已经停止安全维护，镜像仍有 3 个严重、208 个高危漏洞项。本版本只用于隔离内网验证，不能作为正式生产版本。
